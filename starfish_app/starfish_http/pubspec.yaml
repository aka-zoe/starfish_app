name: starfish_http
description: "A new Flutter project."
version: 0.0.1
homepage:

environment:
    sdk: '>=3.4.0 <4.0.0'
    flutter: ">=1.17.0"

dependencies:
    flutter:
        sdk: flutter
    starfish_utils:
        path: ../starfish_utils
    starfish_route:
        path: ../starfish_route
    # 网络请求
    dio: ^5.3.4
    # cookie持久化
    dio_cookie_manager: ^3.1.1
    cookie_jar: ^4.0.8
    # 长连接
    web_socket_channel: 2.4.0
    # toast提示
    oktoast: ^3.3.1
dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^3.0.0

flutter:
